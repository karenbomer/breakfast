<form method="post" action="{{ path('app_backoffice_post_delete', {'id': post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-outline-light d-flex text-uppercase">Supprimer</button>
</form>
